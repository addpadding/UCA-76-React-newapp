import React, { Component } from 'react'

class RenderProps extends Component {
    render() {
        return (
            <div>
                {this.props.hamadan}
            </div>
        )
    }
}

export default RenderProps
